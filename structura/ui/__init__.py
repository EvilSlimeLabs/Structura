"""The desktop window, and what it needs to draw itself.

The window, the fonts and glyphs it draws with, and the language tables that
label it. `structura_core` is what it drives; nothing here is needed to build a
pack from the command line.
"""
