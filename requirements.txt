# Runtime dependencies for the desktop program (structura.py).
# Build tooling lives in requirements-build.txt.
#
# tkinter ships with CPython and is not installable from PyPI. On Linux it
# usually needs a system package such as python3-tk.

nbtlib>=2.0.4,<3
numpy>=1.24.2
Pillow>=9.4.0
requests>=2.32.0
ujson>=5.10.0
